👤 USER (click trên map)
        ↓
🌐 FRONTEND (Leaflet / JS)
        ↓  (fetch POST /api/path)
🚪 API (routes.py)
        ↓
🧠 SERVICE (pathfinding_service.py)
        ↓
⚙️ ALGORITHM (astar.py)
        ↓
🧱 GRAPH (models + data)
        ↓
⚙️ ALGORITHM tính xong
        ↓
🧠 SERVICE format lại
        ↓
🚪 API trả JSON
        ↓
🌐 FRONTEND nhận data
        ↓
🗺️ VẼ ĐƯỜNG ĐI
